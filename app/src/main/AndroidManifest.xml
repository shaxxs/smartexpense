<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.mathi.smartexpense">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/icon_app_blue"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/icon_app_blue"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".MainLoginActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".NewERActivity" />
        <activity android:name=".PasswordActivity" />
        <activity android:name=".RefundTrackerActivity" />
        <activity android:name=".StatsActivity" />
        <activity android:name=".NewExpenseActivity" />
        <activity android:name=".DashboardActivity" />
        <activity android:name=".UserActivity" />
        <activity android:name=".ExpenseReportActivity" />
        <activity android:name=".ERDetailsActivity" />
        <activity android:name=".ExpenseDetailsActivity" />
        <activity android:name=".ExpenseMoreDetailsActivity" />
        <activity android:name=".ProofActivity"></activity>
    </application>

</manifest>